name: Build and Test

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  verify:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm run test

      - name: Build (ESM CLI)
        run: npm run build

  windows-app:
    runs-on: windows-latest
    needs: verify
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build Electron app (portable EXE)
        run: npm run package:win

      - name: Create source archive
        shell: bash
        run: |
          git archive --format=zip --output release/source-code.zip HEAD -- . ':(exclude)node_modules' ':(exclude)node_modules/**'

      - name: Bundle release zip
        shell: pwsh
        run: |
          $exePath = Get-ChildItem -Path release -Filter "*.exe" | Select-Object -First 1 -ExpandProperty FullName
          Compress-Archive -Path $exePath, release/source-code.zip `
            -DestinationPath release/slidesmith-release.zip

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: slidesmith-release
          path: release/slidesmith-release.zip
